package interfaceEx;


abstract public interface InterfaceEx1 {		//every file have only one public class/constructor.

	abstract public int addition(int a,int b);	// All methods in interface are abstract. so no need to use keyword "abstract" for class and methods.

}
 

interface InterfaceEx2 {		//this is different constructor
	
	abstract public int subtraction(int a,int b);	//no need to use keyword "abstract" for class and methods.

}


//interface same as class, instead of "class" u can use "Interface". 